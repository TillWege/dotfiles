[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = Tillwege
	email = tillwege@gmail.com
[core]
	autocrlf = input
    excludesFile = ~/.gitignore
[init]
    defaultBranch = main